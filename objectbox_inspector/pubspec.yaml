name: objectbox_inspector
description: "A visual interface for all the classes annotated with @Entity in ObjectBox."
version: 0.0.4
publish_to: 
homepage: https://github.com/ccosnean/objectbox-inspector/tree/main/objectbox_inspector

environment:
  sdk: ^3.0.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  objectbox: ^4.0.3
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ">=3.0.0 <4.0.0"
