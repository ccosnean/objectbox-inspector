name: example
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.1.0

dependencies:
  flutter:
    sdk: flutter
  objectbox: ^4.3.0
  path_provider: ^2.1.2
  path: ^1.8.3
  objectbox_flutter_libs:
  freezed_annotation:
  json_annotation:
  objectbox_inspector:
    path: ../objectbox_inspector/
  # objectbox_inspector: ^0.0.6

dev_dependencies:
  freezed:
  json_serializable:
  flutter_lints: ^4.0.0
  build_runner: ^2.4.9
  objectbox_generator: ^4.3.0
  # objectbox_inspector_generator: ^0.0.6
  objectbox_inspector_generator:
    path: ../objectbox_inspector_generator/

flutter:
  uses-material-design: true